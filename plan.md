## 产品概述

智能商旅助手是一个基于多Agent协作的对话式商旅服务系统。系统通过自然语言理解用户出差需求，由多个专业Agent协同工作，完成行程规划、住宿预订、宴请安排，并生成报销审批单。

## 核心功能

### 1. 多LLM网关服务

- 统一的LLM调用网关，支持紫东太初kimi k2.5及备用LLM
- 主LLM失败时自动切换备用LLM并重试
- 统一封装返回格式，对上层Agent屏蔽LLM差异

### 2. Dispatcher Agent - 任务调度

- 接收用户所有输入
- 判断请求类型（行程规划/发票处理/Mock数据生成）
- 创建和管理Agent协作工作流
- 协调各Agent之间的消息传递
- 管理对话状态和上下文
- 支持并行调用多个不相关联的专业Agent

### 3. Intent Understanding Agent - 意图识别

- 基于LLM的语义理解能力解析用户输入（非关键词匹配）
- 支持上下文记忆，允许多轮对话中引用之前信息
- 正确识别非标准表述为出差需求
- 通过追问澄清模糊或多重含义的表述

### 4. Intent Understanding Agent - 信息补全追问

- 检查必填信息完整性
- 生成追问列表（每轮最多3个问题）
- 追问信息包括：职级、出差目的、客户位置、出行方式、时间、住宿要求、宴请信息等
- 更新上下文并跳过已回答问题的后续追问

### 5. Compliance Agent - 合规检查

- 维护完整差旅标准规则库（基于公司差旅管理标准）
- **交通合规**：验证飞机舱位、高铁座位是否符合职级标准
- **住宿合规**：按城市等级（一线/新一线/二线/其他）+ 职级验证住宿标准
- **餐饮合规**：验证餐饮补贴和公务接待人均是否超标
- **审批流程合规**：验证差旅申请审批权限
- 对超标项提供替代方案建议
- 生成结构化合规报告

### 6. Transportation Agent - 出行方案推荐与预订

- 搜索航班信息（出发地、目的地、日期）
- 搜索火车票信息
- 调用Compliance Agent验证合规性
- 同时展示火车和高铁方案及符合差标的特价机票
- 展示每个方案的出发/到达时间、历时、费用、合规性
- **用户确认方案后，支持直接下单预订（Mock）**

### 7. Hotel Agent - 住宿方案推荐与预订

- 在目标区域搜索符合要求的酒店
- 调用Compliance Agent验证合规性
- 生成双重推荐：合规推荐+超标区域外替代方案
- 标注酒店与客户位置的距离和最佳交通方式
- **用户确认方案后，支持直接下单预订（Mock）**

### 8. Dining Agent - 宴请方案推荐与预订

- 搜索符合条件的餐厅
- 调用Compliance Agent验证合规性
- 超标时执行双重展示：超标提示+合规推荐
- 推荐匹配宴请人数的菜品
- **用户确认方案后，支持直接预约（Mock）**

### 9. Approval Form Agent - 审批单生成

- 汇总各Agent推荐结果
- 生成包含出差信息、行程详情、住宿/宴请安排、费用汇总、差标对比的审批单
- 支持HTML和PDF格式输出
- 支持打印和电子存档

### 10. Invoice Agent - 发票识别

- 识别发票图片（JPG/PNG）和PDF文件
- 使用OCR和多模态LLM提取发票字段
- 低置信度字段标记为"待确认"
- 支持批量上传多张发票
- 生成报销信息草稿

### 11. Mock Data Agent - Mock数据生成

- 调用web-content-fetcher获取真实数据
- 解析和结构化数据
- 生成1:1比例的Mock数据集
- 以JSON格式输出

### 12. Agent COT框架

- 每个Agent实现独立的COT推理框架
- 四层结构：意图理解层、知识检索层、推理决策层、响应生成层
- 支持展示推理过程（可调试模式）
- 确保COT推理结果可复现

### 13. 语音交互模块（TTS + ASR）

- TTS：集成KittenTTS
- ASR：集成Moonshine（轻量级开源ASR，27-61MB，比Whisper快5倍）
- 支持双向语音交流

### 14. 预订下单功能

当用户确认出行、住宿、宴请方案后，系统支持直接帮助用户完成预订：

- **出行预订**：用户选择航班/火车票后，系统调用预订接口完成下单（Mock模式）
- **酒店预订**：用户选择酒店后，系统调用预订接口完成下单（Mock模式）
- **宴请预约**：用户选择餐厅后，系统调用预约接口完成预约（Mock模式）
- **订单管理**：保存用户订单状态，支持查询和取消

### 15. 数据源获取

- 航班/火车票：web-content-fetcher抓取12306/携程公开数据
- 酒店/餐厅：高德地图POI搜索API + web-content-fetcher
- 发票OCR：紫东太初多模态LLM
- 地理位置：高德地图Web服务API

## 开发范围

- 后端开发（当前阶段）
- 前端界面（后续由用户提供）

## 技术栈选择

### 核心框架

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| API层 | Python + FastAPI | 统一入口、身份认证、请求路由 |
| Agent协作 | Redis Streams | Agent间异步通信、任务分发 |
| 数据存储 | Supabase (PostgreSQL + Redis) | 会话状态、差标规则、审批单存储 |
| LLM集成 | 紫东太初kimi k2.5 + 备用LLM | 统一调用、故障切换 |


### 语音模块

| 模块 | 技术选型 | 特点 |
| --- | --- | --- |
| TTS | KittenTTS | 优质、轻量（用户提供） |
| ASR | Moonshine | 开源离线、27-61MB、比Whisper快5倍、支持中文 |


### 数据源

| 数据类型 | 获取方式 |
| --- | --- |
| 航班/火车票 | web-content-fetcher抓取12306/携程 |
| 酒店/餐厅 | 高德地图POI API + web-content-fetcher |
| 发票OCR | 紫东太初多模态LLM |
| 地理位置 | 高德地图Web服务API |


## 实现方案

### 1. 项目架构

采用分层架构设计：

- **API层**：FastAPI统一入口，处理HTTP/WebSocket请求
- **Agent层**：各专业Agent独立实现，通过消息队列通信
- **COT层**：统一的思维链推理框架
- **服务层**：数据搜索、高德API、LLM调用
- **存储层**：Supabase PostgreSQL + Redis

### 2. Agent协作机制

- 每个Agent是独立的工作进程/类
- 通过Redis Streams消息队列进行异步通信
- Dispatcher Agent作为协调入口
- 支持并行调用多个独立Agent

### 3. COT框架实现

每个Agent内部采用COT框架：

```
Intent Understanding Layer → Knowledge Retrieval Layer → Reasoning Decision Layer → Response Generator Layer
```

### 4. LLM网关设计

- 抽象Provider接口
- 实现紫东太初Provider
- 实现Fallback Provider
- 自动故障切换机制
- 请求限流与重试

### 5. 差旅标准规则（基于公司差旅管理标准）

#### 5.1 职级定义

- **基层员工**：专员/助理
- **主管/资深专员**：主管、资深专员
- **经理级**：经理
- **总监级**：总监
- **副总/高管**：副总及以上

#### 5.2 境内交通标准

**飞机舱位**：

| 职级 | 舱位标准 | 适用场景 |
| --- | --- | --- |
| 基层员工 | 经济舱折扣票 | 里程≥1000km，且高铁时长≥6小时 |
| 主管/资深专员 | 经济舱 | 里程≥800km，且高铁时长≥5小时 |
| 经理级 | 经济舱/高端经济舱 | 无里程限制 |
| 总监级 | 商务舱折扣票 | 时长≥8小时，或月度出差≥4次 |
| 副总/高管 | 商务舱/头等舱 | 无限制 |


**高铁/动车/火车**：

| 职级 | 座位标准 |
| --- | --- |
| 基层员工 | 二等座 |
| 主管/资深专员 | 二等座（时长≥8小时可申请一等座） |
| 经理级 | 一等座（时长≥10小时可申请商务座） |
| 总监及以上 | 一等座/商务座 |


#### 5.3 境内住宿标准（元/晚）

| 职级 | 一线城市 | 新一线城市 | 二线城市 | 其他城市 |
| --- | --- | --- | --- | --- |
| 基层员工 | 400 | 350 | 300 | 260 |
| 主管/资深专员 | 500 | 450 | 400 | 350 |
| 经理级 | 650 | 600 | 550 | 500 |
| 总监级 | 800 | 750 | 700 | 650 |
| 副总/高管 | 1200（可升级） | 1000 | 900 | 800 |


**一线城市**：北京、上海、广州、深圳
**新一线城市**：杭州、成都、重庆、武汉、南京、西安、长沙、苏州、天津、东莞、青岛、沈阳、郑州、昆明等

#### 5.4 境内餐饮补贴标准（元/天）

| 职级 | 一线城市 | 新一线城市 | 二线城市 | 其他城市 |
| --- | --- | --- | --- | --- |
| 基层员工 | 120 | 100 | 90 | 80 |
| 主管/资深专员 | 150 | 130 | 110 | 100 |
| 经理级 | 200 | 180 | 160 | 140 |
| 总监及以上 | 300 | 280 | 260 | 240 |


**公务接待宴请标准**：

- 一线城市：人均≤300元
- 新一线城市：人均≤260元
- 其他城市：人均≤200元

#### 5.5 其他补贴

- **通讯补贴**：境内出差月上限50元；境外出差单日上限80元
- **偏远地区补贴**：西藏/新疆/青海等额外50元/天
- **节假日补贴**：法定假日额外100元/天
- **夜间抵达补贴**：22:00后抵达额外30元/次

#### 5.6 报销时限

- 返程后5个工作日内提交报销
- 逾期每天扣除1%（最高10%），逾期15天不予报销

#### 5.7 差旅申请审批权限

- 基层员工：直属主管→部门经理
- 主管/资深专员：部门经理→总监
- 经理级：总监→分管副总
- 总监及以上：分管副总→总经理/董事长

### 6. 预订下单流程

当用户确认方案后，系统触发预订流程：

```
用户确认方案 → Intent识别BOOKING意图 → Dispatcher分发到对应Agent
     ↓
Transportation/Hotel/Dining Agent执行预订（Mock）
     ↓
生成订单记录 → 更新Trip状态 → 返回预订结果给用户
     ↓
等待用户确认所有方案 → 生成审批单
```

- 预订采用Mock模式，模拟真实预订流程
- 订单数据存储到Supabase数据库
- 支持订单状态查询（待确认/已确认/已取消）

## 目录结构

```
slv/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI入口
│   ├── config.py               # 配置管理
│   ├── api/                    # API路由
│   │   ├── routes/
│   │   │   ├── chat.py         # 对话接口
│   │   │   ├── invoice.py      # 发票接口
│   │   │   ├── approval.py     # 审批单接口
│   │   │   └── voice.py        # 语音接口
│   │   └── middleware/
│   │       └── auth.py
│   ├── agents/                 # Agent实现
│   │   ├── base.py             # Agent基类
│   │   ├── dispatcher.py       # 调度Agent
│   │   ├── intent.py            # 意图理解Agent
│   │   ├── transportation.py    # 出行Agent（含预订功能）
│   │   ├── hotel.py             # 酒店Agent（含预订功能）
│   │   ├── dining.py            # 宴请Agent（含预约功能）
│   │   ├── compliance.py        # 合规检查Agent
│   │   ├── invoice.py           # 发票Agent
│   │   ├── approval.py          # 审批单Agent
│   │   └── mock_data.py         # Mock数据Agent
│   ├── booking/                # 预订模块
│   │   ├── base.py             # 预订基类
│   │   ├── flight_booking.py    # 航班预订（Mock）
│   │   ├── train_booking.py     # 火车票预订（Mock）
│   │   ├── hotel_booking.py     # 酒店预订（Mock）
│   │   └── restaurant_booking.py # 餐厅预约（Mock）
│   ├── cot/                    # COT框架
│   │   ├── engine.py            # COT引擎
│   │   ├── layers/              # COT各层
│   │   └── prompts.py           # 提示词模板
│   ├── llm/                    # LLM网关
│   │   ├── gateway.py           # 网关服务
│   │   └── providers/           # LLM提供商
│   │       ├── zidongtaichu.py  # 紫东太初
│   │       └── fallback.py       # 备用LLM
│   ├── services/               # 服务层
│   │   ├── search/              # 搜索服务
│   │   ├── amap.py              # 高德地图服务
│   │   └── web_fetcher.py       # 网页抓取
│   ├── voice/                  # 语音模块
│   │   ├── tts.py               # TTS服务
│   │   ├── asr.py               # ASR服务
│   │   └── pipeline.py          # 语音交互流程
│   ├── storage/                # 存储层
│   │   ├── supabase.py          # Supabase客户端
│   │   └── redis_client.py      # Redis客户端
│   ├── queue/                  # 消息队列
│   │   ├── publisher.py
│   │   └── consumer.py
│   └── utils/
│       ├── logger.py
│       └── validators.py
├── tests/
├── scripts/
│   └── init_db.sql            # 数据库初始化SQL
├── config/
│   └── .env.example
├── requirements.txt
└── README.md
```